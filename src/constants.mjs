export const SERVICE_NAME = 'octopus-upstream-http-bridge';
export const LOG_PREFIX = '[octopus-bridge]';

export const DEFAULT_MAX_BODY_BYTES = 10 * 1024 * 1024;
export const DEFAULT_UPSTREAM_TIMEOUT_MS = 300000;
export const DEFAULT_HEADERS_TIMEOUT_MS = 65000;
export const DEFAULT_REQUEST_TIMEOUT_MS = 300000;
export const DEFAULT_KEEP_ALIVE_TIMEOUT_MS = 5000;
export const DEFAULT_SHUTDOWN_TIMEOUT_MS = 15000;
